info (application)
    TITLE           - assembly.title
    DESCRIPTION     - assembly.description
    CONTACT         - взять имя сервиса типа @sl-fubar? а еще на SL ссылку сгенерить ENV::NAMESPACE (new-chronos)
    VERSION         - хардкод. это же версия апи а не сервиса

method (action)
    SUMMARY:        action.summary
    DESCRIPTION:    action.remarks
    RESULT (swag):  action.response: 200, text  - в сваггере видно описание ответа в зависимости от кода, у нас всегда 200

content descriptor
    SUMMARY:        ? - надо пробросить из типа, если есть. для солянки из параметров будет пусто.
    DESCRIPTION:    ? - надо пробросить из типа, если есть. для солянки из параметров будет пусто.

schema (type)
    TITLE:          type.summary
    DESCRIPTION:    type.remarks

property
    TITLE:          type.summary
    DESCRIPTION:    prop.summary
    EXAMPLE:        prop.example

надо как-то прокинуть инфу о параметрах из метода в сгенерированный класс
еще прочекать как себя ведут nullable и default параметры и проперти...

! получается, аннотации к типу контроллера никуда не идут. в сваггере они используются для action-а, если у него не нашлось нужных аннотаций

подумать над разделением публичного и непубличного кода - что-то специфично для точки, например ссылки на SL